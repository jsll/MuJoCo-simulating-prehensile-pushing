# Create a yaml file with the following content:
# The name of an object mesh file
# the name of a numpy trajectory file

object: 'L-shape'
mesh_file: 'L-shape.stl'
trajectory_file: 'L_shape_trajectory_1.npy'
